<div class="container">
  <%= link_to 'Create a list', new_list_path, class:'btn btn-flat' %>
  <% @lists.each do |list| %>
    <%= list.name %>
    <%= link_to 'Details', list_path(list) %>
  <% end %>
</div>
